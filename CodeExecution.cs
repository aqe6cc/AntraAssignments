using System;

int max=500;
for (byte i = 0; i < max; i++)
{ 
    Console.WriteLine(i);
    }

/* Answers:

When I ran the code, it creates an infinite loop bug.
To prevent this, I could add a code to say if i equals to the max value of byte, I should make the console say about to overflow. */


